/**
 * useAuth hook - Convenience re-export of AuthContext
 * 
 * Provides easy access to authentication state and helpers
 */

export { useAuth } from '@/contexts/AuthContext';
export type { User, Permission } from '@/contexts/AuthContext';
