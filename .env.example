# Trading Algorithm Document Analyzer - Environment Variables Template
#
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual values (NEVER commit .env to git)
# 3. OR use Doppler: doppler run -- python main.py
#
# See docs/decisions/012-doppler-secret-management.md for details

# ============================================================================
# AI PROVIDER API KEYS (Required)
# ============================================================================

# Google Gemini API Key
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Anthropic Claude API Key
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# OpenAI API Key (Optional - for multi-model support)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Alternative naming convention (some modules use these)
AI_INTEGRATIONS_GEMINI_API_KEY=your-gemini-api-key-here
AI_INTEGRATIONS_OPENAI_API_KEY=your-openai-api-key-here

# ============================================================================
# DATABASE CONFIGURATION (Required)
# ============================================================================

# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
# Local dev example: postgresql://docsense:docsense_local_dev@localhost:5432/docsense
DATABASE_URL=postgresql://user:password@localhost:5432/docsense

# Database connection pool settings (Optional)
DB_POOL_MIN_SIZE=2
DB_POOL_MAX_SIZE=10

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================

# Environment: development, staging, production
ENVIRONMENT=development

# Application port (default: 8000)
PORT=8000

# API base path
API_BASE_PATH=/api/v1

# Enable debug mode (development only)
DEBUG=true

# ============================================================================
# SECURITY SETTINGS
# ============================================================================

# Secret key for JWT/session signing
# Generate with: openssl rand -hex 32
SECRET_KEY=your-secret-key-here

# CORS allowed origins (comma-separated)
# Format: Comma-separated list of allowed origins (no spaces around commas recommended)
# Examples:
#   Development: http://localhost:5000,http://localhost:3000
#   Production:  https://app.example.com,https://www.example.com
#
# IMPORTANT SECURITY NOTES:
# 1. NEVER use "*" (wildcard) in production - it's a security vulnerability
# 2. Wildcard "*" cannot be used with allow_credentials=True (CORS policy violation)
# 3. Always specify exact origins including protocol (http/https) and port
# 4. Do not include trailing slashes in origins
# 5. In production, this value must be set to your actual frontend domain(s)
#
# The application will:
# - Fail to start in production if set to "*"
# - Log warnings in development if using "*"
# - Automatically disable credentials if "*" is used in development
CORS_ORIGINS=http://localhost:5000,http://localhost:3000

# ============================================================================
# STORAGE CONFIGURATION (Optional)
# ============================================================================

# Document upload directory
UPLOAD_DIR=./uploads

# Max upload size in bytes (default: 10MB)
MAX_UPLOAD_SIZE=10485760

# ============================================================================
# AI ANALYSIS SETTINGS (Optional)
# ============================================================================

# Default AI provider: gemini, claude, openai
DEFAULT_AI_PROVIDER=gemini

# AI request timeout (seconds)
AI_REQUEST_TIMEOUT=120

# Max retries for AI requests
AI_MAX_RETRIES=3

# ============================================================================
# LOGGING CONFIGURATION (Optional)
# ============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log format: json, text
LOG_FORMAT=json

# Enable AI response debugging (saves responses to files)
SAVE_AI_RESPONSES=false

# ============================================================================
# EXTERNAL SERVICES (Optional)
# ============================================================================

# Sentry DSN for error tracking
SENTRY_DSN=

# Analytics/monitoring
ANALYTICS_ENABLED=false

# ============================================================================
# FRONTEND CONFIGURATION (Client-side only)
# ============================================================================

# These should be in client/.env and prefixed with VITE_
# See client/.env.example for frontend-specific variables

# ============================================================================
# DEVELOPMENT TOOLS (Development only)
# ============================================================================

# Doppler token for local development (alternative to manual .env)
# Get from: doppler setup
DOPPLER_TOKEN=

# Enable hot reload in development
HOT_RELOAD=true

# Enable API documentation
ENABLE_DOCS=true
